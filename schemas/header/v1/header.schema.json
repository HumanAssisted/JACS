{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://hai.ai/schemas/header/v1/header.schema.json",
  "title": "Header",
  "description": "The basis for a JACS document",
  "type": "object",
  "properties": {
    "id": {
      "description": "uuid v4 string",
      "type": "string",
      "format": "uuid"
    },
    "registrars": {
      "type": "array",
      "description": "Signing authorities agent is registered with.",
      "items": {
        "description": "creator signature",
        "$ref": "../../components/signature/v1/signature.schema.json"
      }
    },
    "permissions": {
      "type": "array",
      "description": "array of permissions",
      "items": {
        "description": "document permissions",
        "$ref": "../../components/permission/v1/permission.schema.json"
      }
    },
    "version": {
      "description": "Version id of the object. uuid v4 string ",
      "type": "string",
      "format": "uuid"
    },
    "versionDate": {
      "description": "Date ",
      "format": "date-time",
      "type": "string"
    },
    "selfSignature": {
      "description": "Signature of of every field except this one and the hashing field by the creating/editing agent.",
      "$ref": "../../components/signature/v1/signature.schema.json"
    },
    "versionRegistration": {
      "description": "Signature of this version by the registrar",
      "$ref": "../../components/signature/v1/signature.schema.json"
    },
    "previousVersion": {
      "description": "Previous Version id of the object. If blank, it's claiming to be the first ",
      "type": "string",
      "format": "uuid"
    },
    "originalVersion": {
      "description": "Original Version id of the object. When documents are copied without merging, this becomes the way to track them.",
      "type": "string",
      "format": "uuid"
    },
    "sha256": {
      "description": "Hash of every field except this one. During  updates and creation hash is the last thing to occur, as it includes the signature. Not immediatly required, but eventually required.",
      "type": "string"
    },
    "eval": {
      "description": "A description of how the document should be evaluated.",
      "type": "string"
    }
  },
  "required": [
    "id",
    "version",
    "versionDate",
    "originalVersion"
  ]
}
