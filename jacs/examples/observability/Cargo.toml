[package]
name = "jacs-observability-demo"
version = "0.1.0"
edition = "2021"

[dependencies]
jacs = { path = "../../" }
tracing = "0.1"
serde_json = "1.0"
chrono = { version = "0.4", features = ["serde"] }

# Simple HTTP server without tokio
tiny_http = "0.12"
ctrlc = "3.2"
