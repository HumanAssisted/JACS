pub mod agent;
pub mod crypt;
pub mod loaders;
pub mod schema;
pub mod testtools;

pub fn create_agent() {}

pub fn update_agent() {
    // load original
    // update fields (new)
    // diff fields
    // update version
    // validate new
    // overwrite old
}

// create resource (omnipotent)
// create task
// create action
// create decision (omnipotent)
// update task from decision
// update task (version)
