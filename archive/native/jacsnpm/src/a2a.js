/**
 * JACS A2A (Agent-to-Agent) Protocol Integration for Node.js
 *
 * Re-exports from the TypeScript-compiled module at the package root.
 * This shim ensures backward compatibility for existing `require('./a2a')`
 * and `require('../src/a2a')` paths.
 */
module.exports = require('../a2a.js');
