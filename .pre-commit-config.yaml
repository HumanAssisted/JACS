repos:
  - repo: local
    hooks:
      - id: cargo-fmt
        name: Check active workspace formatting
        entry: cargo fmt --all -- --check
        language: system
        pass_filenames: false
      - id: workspace-boundary
        name: Check portable dependency boundary
        entry: python3 scripts/check_workspace_boundary.py
        language: system
        pass_filenames: false
